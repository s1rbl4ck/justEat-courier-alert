services:
  app:
    build: .
    ports:
      - '127.0.0.1:8080:8080'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # You can add a named volume for persistent logs or state if required
    # volumes:
    #   - ./data:/data
